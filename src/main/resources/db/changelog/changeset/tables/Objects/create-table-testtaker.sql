CREATE TABLE IF NOT EXISTS testtaker
(
    testtaker_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    testtaker_fio VARCHAR(128) NOT NULL,
    testtaker_email VARCHAR(55) NOT NULL UNIQUE
);